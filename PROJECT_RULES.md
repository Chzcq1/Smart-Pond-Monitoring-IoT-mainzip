Rules

1. Never redesign completed UI unless requested.

2. Never rename existing components unless necessary.

3. Always build reusable components.

4. Keep folder structure clean.

5. Separate UI from future API layer.

6. Every page must support future Google Apps Script integration.

7. Do not implement features outside the current sprint.

8. Always preserve existing functionality.